package frc.robot.subsystems;

public class ArticulatedIntake {}
    
// Pass hardware into constructor 
// Pass in the desired angle 

/*private final double ACCEPTABLE_RANGE = 5.0; 
private double currentAngle = findCurrentAngle(); 
    public double findCurrentAngle(double getPositionIntakeEncoder(), double Angle) {}

    public void moveToNewAngle(double desiredAngle) {
        if (currentAngle <= desiredAngle + ACCEPTABLE_RANGE || currentAngle >= desiredAngle - ACCEPTABLE_RANGE) {
            
        } else { 

        }
    }
}*/
